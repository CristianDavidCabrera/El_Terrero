{% extends 'base.html.twig' %}

{% block title %}Team{% endblock %}

{% block body %}
    <h1>Teams</h1>
{# {% for team in teams %} #}
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ team.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ team.name }}</td>
            </tr>
            <tr>
                <th>Division</th>
                <td>{{ team.division }}</td>
            </tr>
           {#  <tr>
                <th>Id_fighter</th>
                <td>{{ team.idFighter }}</td>
            </tr> #}
        </tbody>
    </table>

        <a href="{{ path('app_team_index') }}"  class="btn btn-lg btn-primary"   >Back to list</a>
        <a href="{{path('app_team_showYourTeams', {'clubId':app.user.club.id})}}">Back teams</a>
        <a href="{{ path('app_team_edit', {'id': team.id}) }}">edit</a>

    {{ include('team/_delete_form.html.twig') }}
{#     {% endfor %} #}
{% endblock %}


